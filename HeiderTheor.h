#pragma once

#include <vector>
#include "Constants.h"

int get_weight(std::vector<int> node1, std::vector<int>node2);
unsigned long long int factorial(unsigned long long int n);
unsigned long long int binomial(unsigned long long int n, unsigned long long int k);
unsigned long long get_minus_minus_count(int d, bool isPlusPlus=false);